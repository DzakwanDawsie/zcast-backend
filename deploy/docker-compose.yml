services:
  zcast-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: zcast-api
    restart: unless-stopped
    ports:
      - "${PORT:-8424}:8424"
    env_file:
      - ../.env
    volumes:
      - zcast-public:/app/public
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8424/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  zcast-public:
